{% if events %}
{% import "/macros/cards.html.twig" as cards %}
<div class="row row-cols-1 row-cols-sm-2 row-cols-md-3 row-cols-lg-4">
    {% for event in events %}
    {{ cards.grid(event, 'event') }}
    {% endfor %}
</div>
{% else %}
<p class="alert alert-warning" role="alert">Aucun évènement</p>
{% endif %}