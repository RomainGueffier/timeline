{% extends 'base.html.twig' %}

{% block title %}Modifier une catégorie{% endblock %}

{% block container %}
<main role="main" class="flex-shrink-0">
    <div class="container">
        <h2 class="my-2 text-light">Modifier la catégorie <span class="display-4">{{ category.name }}</span></h2>
        <div class="card card-body">
            {{ form(form) }}
        </div>
    </div>
</main>
{% endblock %}
