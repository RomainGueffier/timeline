{% if categories %}
{% for category in categories %}
<div class="custom-control custom-switch">
    <input type="checkbox" class="custom-control-input" category_id="{{ category.id }}" id="customSwitch{{ category.id }}" checked="checked">
    <label class="custom-control-label" for="customSwitch{{ category.id }}">{{ category.name }}</label>
</div>
{% endfor %}
{% else %}
<p>Aucune catégories</p>
{% endif %}
