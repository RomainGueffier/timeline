{% extends 'base.html.twig' %}

{% block title %}Modifier un évènement{% endblock %}

{% block container %}
<main role="main" class="flex-shrink-0">
    <div class="container">
        <h2 class="my-2 text-light">Modifier l'évènement <span class="display-4">{{ event.name }}</span></h2>
        <div class="card">
            <div class="card-body">
                {{ form(form) }}
            </div>
        </div>
    </div>
</main>
{% endblock %}
